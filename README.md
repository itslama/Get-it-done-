# React + Vite
a full-stack React web app featuring Firebase Authentication for secure user login, Firestore real-time cloud database for persistent task storage, with dark/light mode toggle button. The app includes a daily task tracker with progress visualization, a tomorrow planner, and an important notes section.
Deployed on Netlify with full mobile responsiveness.
